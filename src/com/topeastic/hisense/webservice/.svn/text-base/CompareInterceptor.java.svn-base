//package com.topeastic.hisense.webservice;
//
//import java.lang.reflect.Method;
//
//import org.springframework.cglib.proxy.MethodInterceptor;
//import org.springframework.cglib.proxy.MethodProxy;
//
//public class CompareInterceptor implements MethodInterceptor {
//
//	 public Object invoke(MethodInvocation invocation) throws Throwable {    
//	        Object result = null;    
//	        String stu_name = invocation.getArguments()[0].toString();    
//	        if (stu_name.equals("dragon")) {    
//	            // 如果学生是dragon时,执行目标方法,    
//	            result = invocation.proceed();    
//	        } else {    
//	            System.out.println("此学生是" + stu_name + "而不是dragon,不批准其加入.");    
//	        }    
//	        return result;    
//	    }    
//
//	 /**
//	  * 所谓AOP就是将分散在各个方法处的公共代码提取到一处，
//	  * 并通过类似拦截器的机制实现代码的动态整合。
//	  * 可以简单地想象成，在某个方法的调用前、执行中、调用后和抛出异常时，动态插入自己的代码。
//	  */
//
//}
